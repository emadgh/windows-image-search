name: Validate updater UI-overhaul compatibility

on:
  push:
    branches: [feat/automatic-github-updates-ui-overhaul]
    paths:
      - '.github/workflows/validate-updater-ui-overhaul.yml'

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/automatic-github-updates-ui-overhaul
          fetch-depth: 0
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - name: Apply compatibility patch
        run: python scripts/apply-updater-ui-overhaul.py
      - name: Format Rust
        run: cargo fmt --all
      - name: Cache Rust build
        uses: Swatinem/rust-cache@v2
      - name: Cargo check
        run: cargo check --all-targets
      - name: Tests
        run: cargo test --all-targets
      - name: Release build
        run: cargo build --release
      - name: Commit integrated updater source
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add Cargo.lock src/main.rs src/ui/mod.rs src/ui/settings_window.rs
          if git diff --cached --quiet; then
            echo 'Updater integration source already current.'
            exit 0
          fi
          git commit -m 'feat(update): integrate updater with UI overhaul'
          git push origin HEAD:feat/automatic-github-updates-ui-overhaul
