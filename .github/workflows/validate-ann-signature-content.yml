name: Validate ANN signature content

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  validate:
    if: github.head_ref == 'fix/ann-signature-embedding-content-v0.3'
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/ann-signature-embedding-content-v0.3
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - uses: Swatinem/rust-cache@v2
      - name: Apply fix
        run: python scripts/apply_ann_signature_content_fix.py
      - name: Format
        run: cargo fmt --all
      - name: Cargo check
        run: cargo check --all-targets
      - name: Future-incompatible warnings are errors
        shell: pwsh
        run: |
          $env:RUSTFLAGS = '-Dfuture-incompatible'
          cargo check --all-targets
      - name: Regression tests
        run: cargo test --all-targets
      - name: Release build
        run: cargo build --release
      - name: Commit validated source
        shell: pwsh
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src\db.rs
          if (git status --porcelain -- src\db.rs) {
            git commit -m "fix: include embedding content in ANN signature"
            git push origin HEAD:fix/ann-signature-embedding-content-v0.3
          }
