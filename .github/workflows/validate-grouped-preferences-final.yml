name: Validate final grouped Preferences

on:
  push:
    branches:
      - ui/grouped-preferences-window

permissions:
  contents: write

jobs:
  validate:
    if: github.actor != 'github-actions[bot]'
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ui/grouped-preferences-window
          fetch-depth: 0
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - name: Cache Rust build
        uses: Swatinem/rust-cache@v2
      - id: fmt
        name: Format check
        continue-on-error: true
        run: cargo fmt --all -- --check
      - id: check
        name: Cargo check
        continue-on-error: true
        run: cargo check --all-targets
      - id: tests
        name: Tests
        continue-on-error: true
        run: cargo test --all-targets
      - id: release
        name: Release build
        continue-on-error: true
        run: cargo build --release
      - name: Record failed validation
        if: steps.fmt.outcome != 'success' || steps.check.outcome != 'success' || steps.tests.outcome != 'success' || steps.release.outcome != 'success'
        shell: bash
        run: |
          cat > .preferences-ci-failure.txt <<EOF
          fmt=${{ steps.fmt.outcome }}
          check=${{ steps.check.outcome }}
          tests=${{ steps.tests.outcome }}
          release=${{ steps.release.outcome }}
          EOF
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .preferences-ci-failure.txt
          git commit -m "ci: record grouped Preferences validation failure"
          git push origin HEAD:ui/grouped-preferences-window
      - name: Clean successful validator
        if: steps.fmt.outcome == 'success' && steps.check.outcome == 'success' && steps.tests.outcome == 'success' && steps.release.outcome == 'success'
        shell: bash
        run: |
          rm .github/workflows/validate-grouped-preferences-final.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A .github/workflows/validate-grouped-preferences-final.yml
          git commit -m "chore: remove successful Preferences validator"
          git push origin HEAD:ui/grouped-preferences-window
      - name: Fail job when validation failed
        if: steps.fmt.outcome != 'success' || steps.check.outcome != 'success' || steps.tests.outcome != 'success' || steps.release.outcome != 'success'
        shell: bash
        run: exit 1
