name: Vendor face models

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/vendor-face-models.yml"
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  vendor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Smoke test
        run: echo "vendor workflow is valid"
