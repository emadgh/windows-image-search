name: Apply UI UX acceptance cleanup

on:
  push:
    branches:
      - feat/ui-ux-overhaul-phase-1
    paths:
      - scripts/apply_ui_ux_acceptance_cleanup.py
      - .github/workflows/apply-ui-ux-acceptance-cleanup.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/ui-ux-overhaul-phase-1
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - name: Apply UX acceptance cleanup
        run: python scripts/apply_ui_ux_acceptance_cleanup.py
      - name: Format Rust
        run: cargo fmt --all
      - name: Commit integration
        shell: bash
        run: |
          if git diff --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src
          git commit -m "fix(ui): complete UX acceptance details"
          git push origin HEAD:feat/ui-ux-overhaul-phase-1
